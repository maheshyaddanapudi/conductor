name: Docker Image CI

on:
  push:
    branches: [ all ]
  pull_request:
    branches: [ all ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build -f docker/server/Dockerfile . --tag conductor:$(date +%s)
